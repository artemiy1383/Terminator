pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "fabric-loom" version "1.10-SNAPSHOT"
    }
}

rootProject.name = "Terminator"